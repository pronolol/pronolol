# Pronolol Backend

## Docker

```bash
# Will launch the db and one call to scrapper
docker compose up
```

```bash
# Lauch only the db
docker compose up db
```

```bash
# Launch the scraper alone
docker compose up scraper
```
